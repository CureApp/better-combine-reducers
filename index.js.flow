// @flow
/*
 * You need redux type definition.
 *
 * npm install -g flow-typed
 * flow-typed install -f 0.32 redux@3.5.2
 *
 */

import type { Reducer, combineReducers } from 'redux'

export type State = any;
export type Action = Object;
export type Reducers = { [key: string]: Reducer<any, any> };

declare export default function better(original: combineReducers<State, Action>): (reducers: Object, initialState: State) => Reducer<any, any>;
